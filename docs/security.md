# Security

- Keep secrets in `.env`, never in Git.
- Keep uploads and generated reports outside Git.
- Use PostgreSQL and controlled object storage in production.
- Use cautious forensic wording; avoid absolute claims.

